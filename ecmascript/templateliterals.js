console.log("with litetals")
    // console.log("with normal method of wrtuing")

for (let num = 1; num <= 10; num++) {
    let tableof = 12;

    // console.log(tableof + " * " + num + " = " + tableof * num);

    console.log(` ${tableof} * ${num} = ${tableof * num}`);
}